//
// Protocol buffers definition for Hello World
//
syntax = "proto3";
package pt.tecnico.hdlt.T25;

// Request payload
message MessageRequest {
  string content = 1;
  string signature = 2;
}

message MessageResponse {
  string content = 1;
  string signature = 2;
}

service MessageService {
  rpc greeting(MessageRequest) returns (MessageResponse);
}
